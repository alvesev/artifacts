Apt Satellites - some helpers and utilities to assist in Debian packages processing:

    - Generate Apt repository from deb-files bulk heap.
    - Extract and pack back a deb-file.
    - Compare two deb-package's versions.

